<?xml version="1.0" encoding="UTF-8"?>
<configuration>

	<property name="USER_HOME" value="${LOG_PATH}" />

	<!-- Define the console log format -->
	 <appender name="ConsoleAppender"
			  class="ch.qos.logback.core.ConsoleAppender">
		<layout class="ch.qos.logback.classic.PatternLayout">
			<Pattern>
				%black(%d{ISO8601}) %highlight(%-5level) [%blue(%t)] %yellow(%C{1.}): %msg%n%throwable
			</Pattern>
		</layout>
	</appender> 

	<appender name="RollingFileAppender"
			  class="ch.qos.logback.core.rolling.RollingFileAppender">
		<file>${USER_HOME}/webcrawler.log</file>
		<encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %level %logger{35} - %mdc %msg%n</Pattern>
		</encoder>

		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<fileNamePattern>${USER_HOME}/webcrawler.log_%d{yyyy-MM-dd}_%i.log</fileNamePattern>
			<!-- Keep 365 days log history -->
			<!-- <maxHistory>365</maxHistory> -->
			<!-- <cleanHistoryOnStart>true</cleanHistoryOnStart> -->
			<timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<!-- Roll the log files when reach size 10 MB -->
				<maxFileSize>10MB</maxFileSize>
			</timeBasedFileNamingAndTriggeringPolicy>
		</rollingPolicy>
	</appender>

	<!-- LOG everything at error level -->
	<root level="error">
		<appender-ref ref="RollingFileAppender" />
		<appender-ref ref="ConsoleAppender" />
	</root>

	<!-- LOG "com.program*" at TRACE level -->
	<logger name="com.program" level="trace" additivity="false">
		<appender-ref ref="RollingFileAppender" />
		<!-- <appender-ref ref="ConsoleAppender" /> -->
	</logger>

</configuration>
